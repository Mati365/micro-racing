/**
 * Performs dot product of two vectors
 *
 * @param {Vector} vec1
 * @param {Vector} vec2
 *
 * @returns {Number}
 */
const dot = (vec1, vec2) => vec1[0] * vec2[0] + vec1[1] * vec2[1];

export default dot;
